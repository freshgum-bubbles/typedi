{
  "$schema": "https://developer.microsoft.com/json-schemas/tsdoc/v0/tsdoc.schema.json",

  // Include the definitions that are required for API Extractor
  "extends": ["@microsoft/api-extractor/extends/tsdoc-base.json"],

  // noStandardTags: false,

  /**
   * freshgum: We add support for a few custom TSDoc tags here that are not ordinarily
   * in API Extractor.  Some of them are related to TypeDoc, which we'll hopefully be
   * able to phase out and replace with API Extractor's Markdown generation.
   */
  "tagDefinitions": [
    // Define a custom tag and specify how it should be parsed
    {
      "tagName": "@group",
      "syntaxKind": "block",
      "allowMultiple": false
    }
  ],

  "supportForTags": {
    // Indicate that the custom tag is supported by your tooling.  (Without this, warnings may
    // be reported saying that the tag is unsupported.)
    "@group": true
  }
}
