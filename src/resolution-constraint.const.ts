export enum ResolutionConstraint {
    Self,
    SkipSelf,
    Many,
    Recursive,
    Optional
}

const {
    Self,
    SkipSelf,
    Many,
    Recursive,
    Optional
} = ResolutionConstraint;

export {
    Self,
    SkipSelf,
    Many,
    Recursive,
    Optional
};
